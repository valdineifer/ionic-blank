import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.vf-starter',
  appName: 'ionic-blank',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
